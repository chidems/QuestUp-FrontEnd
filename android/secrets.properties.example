# Copy this file to `secrets.properties` (which is gitignored) and set your key.
# Gradle injects MAPS_API_KEY into AndroidManifest.xml as ${MAPS_API_KEY} at
# build time. Without a real key the Google Map renders blank.
MAPS_API_KEY=YOUR_MAPS_API_KEY
